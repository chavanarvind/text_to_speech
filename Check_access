import fitz  # PyMuPDF
from PIL import Image
from pyzbar.pyzbar import decode
import io

doc = fitz.open("404177118-LPM-806260-NA-r0.pdf")
 
page = doc.load_page(0)  # first page
 
zoom = 2  # for high quality
mat = fitz.Matrix(zoom, zoom)
pix = page.get_pixmap(matrix=mat)
 
img = Image.open(io.BytesIO(pix.tobytes("png")))

barcodes = decode(img)
for barcode in barcodes:
    print("Type:", barcode.type)




extra information for you: 
QR Code Format:
1. Start of URL: 'htpp://2d.kenvue'
2. API Version '01'
3. Number has 14 Digits
 
Bar Code:
1. Patch to 14 digits with leading zeroes
 
Comparison
Compared QR Digits to Patched Barcode



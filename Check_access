print("\n✅ DEBUG 2: After tru.merge(unique_combinations)")
print(tru.query("CHILD_NM == 'PC-008694'")[[
    "TRU_SPEC_ID", "CHILD_NM", "SRC_SYS_CD", "PLNT_CD", TRU_MAT_NUM
]])

if tru.query("CHILD_NM == 'PC-008694'").empty:
    print("\n❌ ALERT: PC-008694 was DROPPED during merge with unique_combinations!")

previous_round = 0
print('-' * 60)

# ✅ Add this line
current_full_matches = pd.DataFrame(columns=ERP_MATCH_COLS + TRU_MATCH_COLS + [
    SCORE_COL, UNIFIED_MC_COL, 'is_matched', 'Matching Reason', 'confidence', 'match_round'
])

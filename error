import pandas as pd

# Read the CSV with ALL columns as string
df = pd.read_csv("your_file.csv", dtype=str, quotechar='"')

# Clean up: remove literal quotes and spaces from 'comp_mat_no'
df["comp_mat_no"] = df["comp_mat_no"].str.strip().str.strip('"').str.strip()
